⚠️ This folder contains legacy HTML reference files.

These files are NOT used in production.

The real application is built in Next.js under:
- app/
- components/
- lib/
- public/

Do not implement new features using these HTML files.
They are only for visual/content reference when migrating.